/**
 * Export all default components inside this folder, so it's easier to import
 * them in a single linee.
 */
export { default as PhotoContainer } from "./photo-container";
export { default as GroupedContainer } from "./grouped-container";
